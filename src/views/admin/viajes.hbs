<div class="conteiner mx-3">
    <strong>
        <table class="table table-dark table-borderless" id="table" data-search="true" data-toggle="table"
            data-buttons="buttons" data-buttons-class="success" data-show-button-text="true">
            <!--aca esta la tabla :D-->
            <thead>
                <tr>
                    <th data-field="id_usuario">
                        ID
                    </th>
                    <th data-field="chofer" data-sortable="true">
                        Chofer_ID
                    </th>
                    <th data-field="combi" data-sortable="true">
                        Combi_ID
                    </th>
                    <th data-field="origen" data-sortable="true">
                        Origen
                    </th>
                    <th data-field="destino" data-sortable="true">
                        Destino
                    </th>
                    <th data-field="insumos" data-sortable="true">
                        Insumos
                    </th>
                    <th data-field="id" data-formatter="LinkFormatterEdit"><i class="fas fa-user-edit"></i></th>
                    <th data-field="id" data-formatter="LinkFormatterView"><i class="fas fa-user-times"></i></th>
                </tr>
            </thead>
        </table>
</div>
</strong>

<script>
    $(document).ready(function () {
        $table = $('#table');
        $('#table').bootstrapTable('load', []); //aca cuando se carga la pag , lo incializamos y luewgos por el get rellenamos la tabla 
        $.ajax({
            type: 'GET',
            url: 'http://localhost:3000/admin/choferJSON',
            data: {},
            dataType: 'json',
            success: function (data) {
                $('#table').bootstrapTable('load', data);
            }, error: function () {
                alert('Error conexion con el servidor');
            }
        });
    });
    // El value aca no hace nada, nos lo paso Gonza y aca iba lo que hacia cuando tocaban el boton. Ejemplo:
    //function LinkFormatterEdit(value) {
    //return "<a href='/centros/editar/" + value + "' ><button class='btn btn-warning'>Editar</button></a> ";
    // }
    function LinkFormatterEdit(value) {
        return "<a href='#' ><button class='btn btn-primary' >Editar</button></a> ";
    }
    function LinkFormatterView(value) {
        return "<a href='#'><button class='btn btn-danger'>Borrar</button></a> ";
    }
    function buttons() {
        return {
            btnAdd: {
                text: 'Agregar viaje',
                icon: 'fa fa-user-plus',
                event: function () {
                    alert('TIENE QUE HACER ALGO PARA AGREGARLO')
                },
                attributes: {
                    title: 'Agregar un nuevo chofer',
                }
            }
        }
    }
</script>